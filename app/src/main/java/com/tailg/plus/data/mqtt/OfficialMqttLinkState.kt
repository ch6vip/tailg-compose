package com.tailg.plus.data.mqtt

/**
 * Observable MQTT link state for the control UI.
 * Dart `enum OfficialMqttLinkState { disconnected, connecting, connected }`.
 */
enum class OfficialMqttLinkState { DISCONNECTED, CONNECTING, CONNECTED }
